# DB_CONFIG
DB_USER = "YOUR_DB_USERNAME"
DB_PASSWORD = "YOUR_DB_PASSWORD"
DB_HOST = "YOUR_DB_HOST"
DB_PORT = "YOUR_DB_PORT"
DB_NAME = "ai_marketing_agent"

# LLM (Claude preferred if both are set; Gemini is the free-tier alternative - see
# src/utils/llm_provider.py)
ANTHROPIC_API_KEY = "YOUR_ANTHROPIC_API_KEY"
GEMINI_API_KEY = "YOUR_GEMINI_API_KEY"

# EMAIL DISPATCH - Mailgun (leave unset to keep email_sender_tool in dry_run mode)
MAILGUN_API_KEY = "YOUR_MAILGUN_API_KEY"
# Sandbox domains (sandboxXXXX.mailgun.org, free tier) can only send to Authorized
# Recipients you've added + verified in the Mailgun dashboard.
SANDBOX_DOMAIN = "YOUR_SANDBOX_DOMAIN.mailgun.org"
BASE_URL = "https://api.mailgun.net"
MAILGUN_FROM_EMAIL = "MarketMind AI <mailgun@YOUR_SANDBOX_DOMAIN.mailgun.org>"

# MLFLOW
MLFLOW_TRACKING_URI = "sqlite:///mlflow.db"